services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: db
      POSTGRES_PASSWORD: P@ssw0rd
    ports:
      - '5432:5432'
  mysql:
    image: mysql:9
    environment:
      MYSQL_DATABASE: db
      MYSQL_ROOT_PASSWORD: P@ssw0rd
    ports:
      - '3306:3306'
